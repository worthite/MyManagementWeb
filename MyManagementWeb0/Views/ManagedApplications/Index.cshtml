@model IEnumerable<ManagedApps>

@{
    ViewBag.Title = "Index";
}


<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @item.value
        </td>
    </tr>
}

</table>
